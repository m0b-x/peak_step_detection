name: study_step_detection
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  provider: ^6.1.4
  path_provider: ^2.1.5
  sensors_plus: ^6.1.1
  simple_kalman: ^1.0.2
  iirjdart: ^0.1.0
  fftea: ^1.5.0+1
  csv: ^6.0.0
  path: ^1.9.1
  open_filex: ^4.1.0
  permission_handler: ^12.0.0+1
  external_path: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: 0.14.3

flutter_icons:
  android: true
  ios: true
  image_path: "assets/icon/app_icon.png"
  remove_alpha_ios: true

flutter:
  uses-material-design: true
